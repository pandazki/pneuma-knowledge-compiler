__pycache__/
*.pyc
*.pyo
.venv/
# The personal edition owns its environment; its uv.lock ships like the root lock.
/personal/.venv/
!/personal/uv.lock
.pytest_cache/
.ruff_cache/
dist/
node_modules/
*.tsbuildinfo
.env
.DS_Store

# Editor / merge detritus — never part of a change, and easy to stage by accident.
*.swp
*~
*.orig
*.rej
.idea/

# Root canonical git repos (per-user, large and rebuildable).
/data/

# Per-example local state: credentials and raw run output, kept locally, never shipped.
examples/opc/.env
examples/opc/var/

# evals/ runtime. The benchmark corpora are third-party (LoCoMo is CC BY-NC; the
# EverMemBench code repo ships no LICENSE) and are fetched, never vendored; run outputs and
# the derived per-conversation configs are large and regenerable.
evals/.env
evals/datasets/
evals/runs/
evals/*/.generated/
evals/*/strategist/
evals/*/scenario.strategist.yaml

# Pneuma workspace (project config + session state) — local, not part of the codebase.
.pneuma/

# Local-only private datasets: real personal/corporate material used for local
# experiments (raw captures, prepared payloads, prep scripts, derived memory).
# NEVER shipped — the open repository carries only synthetic fixtures.
/local/

# 设计工具的本机工作状态（不随仓库发布）
.impeccable/
/archive/

# keyed .env variants (a real key almost reached a public commit once — never again)
.env.keyed

# Agent session logs and one-wave handoff notes. Useful while a wave is in flight, never a
# product asset: a session transcript is not documentation, and a root-level design or summary
# note with no bilingual mirror contradicts the docs convention and duplicates the authority
# under docs/design/. Durable content belongs in docs/, in both languages.
*.log
/ENGINE-DESIGN.md
/ENGINE-CONSOLE-UI-BRIEF.md
/SUMMARY-*.md
/CODEX-*.md
/.codex-review*

# The console dist the personal edition serves is a built artifact (scripts/personal_console_dist.sh)
personal/src/pkc_personal/console/dist/
.pnpm-store/

# The tray's build output (personal/desktop/.gitignore covers it for git; the hygiene walker asks git from the root)
personal/desktop/src-tauri/target/
personal/desktop/node_modules/
personal/desktop/dist/
