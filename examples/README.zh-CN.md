# 示例

[English](README.md) | **简体中文**

刻意只留一个示例：[`opc/`](opc/README.zh-CN.md)——一个完整的知识库项目，就是 scaffold 流程在一份合成语料上跑完的样子：由自主代理端到端建成，连编译好的库一起随仓发布。不用 API key 就能浏览；可以带上你的参数重新编译；也可以把同一份数据交给你自己的代理，把整段旅程复现一遍。
