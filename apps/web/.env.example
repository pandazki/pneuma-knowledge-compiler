# pneuma-knowledge UI → service API base URL.
# Empty (default): the browser calls same-origin /v1/... which the vite dev server
# proxies to the local service (scripts/dev-api.sh, 127.0.0.1:18000). Set this only
# when the API lives on a different origin (e.g. a deployed service).
VITE_API_BASE=
