---
# 编译契约的机器可读部分：skill 标识 + 主体族的路径模板。
# path_templates 声明库的目录布局，与正文第 1 节的主体族一一对应；
# 改这里就是改库的骨架（只影响以后的编译，已入册的文档不会被改写）。
skill_id: {{SKILL_ID}}
# 重写契约后改个新版本号，注册时以新版本生效——旧文档不迁移，只影响以后的编译。
version: app-v1
path_templates:
  - memory/profile.md
---

# 编译契约：这个库替你记什么

<!-- 这份契约是你的库的宪法：它教编译模型判断——在你的业务里，什么值得长期记住、
     记到哪一页。正文会进入编译模型的 system prompt；像这样的 HTML 注释在注册时
     会被剥掉，只留给编辑契约的你。

     机制层的规矩不归这里管，框架会强制执行：每条入册的 claim 必须带可回溯的
     [cite] 引用、锚点一经分配不可变、滚动归档的冻结历史卷只读。契约只写判断力。

     每一节都有引导问题（HTML 注释）。标着 TODO 的地方，通读你自己的材料之后
     用你的答案整段写掉——不要照抄任何示例：别人的领域判断不可能知道
     你的领域里什么算高价值、什么算噪声。

     这份模板背后的完整实践（类型→隐含用法的推导、主体粒度、入册判据、验收环）
     写在框架仓库的 docs/guides/compile-contract.zh-CN.md，那是这件事的唯一权威；
     本文件只是照着填的骨架。 -->

## 0. 这个库是谁的、为了什么

<!-- 问自己：谁是这批材料的所有者？半年后他最可能回头查什么——
     发生过什么？谁负责？什么时候？后来怎么变的？把答案写成两三句话。 -->

TODO：写下这座库的所有者和他回头最常查的东西。

## 1. 主体族：什么东西立自己的一页

<!-- 这里声明你的主体族——什么东西在你的业务里会长期独立演化？
     一个能独立演化的东西 = 一份文档；一个筐装所有会把库压塌。
     对每个族回答：它收什么？对应 frontmatter 里哪个路径模板？
     什么时候值得为一个新主体开新页（出现一次就开？还是反复出现才开）？
     写完把 frontmatter 的 path_templates 改成和这里一一对应。

     如果你给其中某一族启用了 people 组件（engine.yaml 里的 components / people_family），
     这一族还欠一条判断：在你的领域里，什么算这个人的一种称呼。框架负责报出证据——每份
     来源下会列出全库对在场者集中使用的称呼词——并要求每一项在本轮被裁定：是这个人的称呼
     就写进 aliases 字段，不是就用 decline_alias 说明理由（这个回答只作用于本轮，什么都不
     存；这一页被写下之后，同一个词不会再问第二次）。尊称（头衔加姓，换个人坐这个位子也
     这么叫）通常不是别名，昵称和花名往往是。写法见 docs/guides/compile-contract.zh-CN.md §7。 -->

TODO：列出你的主体族，每族一条：收什么、路径模板、开新页的判据。

## 2. 什么值得入册

<!-- 每次只问一个问题：这条信息，将来会被用于行动、判断、解释、协作或复盘吗？
     会——入册；不会——留在原文和检索层就够了（它们不会丢）。
     再往深一层：不同类型的知识天然蕴含各自的未来用途——事件蕴含时间线，
     项目蕴含持续的进展记录，协作者蕴含不断追加的协作日志。不预设清单，
     从你的数据本身推理出这些用途，并随材料变化演进；没入册的事实答不了——
     这些用途依赖的事实必须是一等公民。
     同一个推理也适用于名字：日常语料里同一个人/项目常有昵称、头衔、缩写、代号——
     一个主体带着多个名字出现，就蕴含一条别名记录：有意识地在其页上标注"也称 X"，
     否则同一主体的故事会被名字劈开，将来的检索和归属都对不上号。 -->

TODO：写两三条你的业务里最典型的「值得」与「不值得」。

## 3. 证据与口径

<!-- 你的材料里，谁说的话算数？转述、提案、模型/工具的输出，和所有者拍板的决定
     怎么区分？（转述≠同意，提案≠决策，跑通≠验收。）

     有一类来源是所有者对这座库本身说的话：`owner-dialogue/v1` 是所有者直接陈述的一次订正、
     一条指示或一次补充，因此它对某条既有 claim 的说法，就是关于那条 claim 的证据——
     引用这段陈述去订正或取代那条 claim，而不是在它旁边再记一条自相矛盾的。 -->

TODO：写下你的领域里「什么算数、什么只是说说」的分界。

## 4. 时间

<!-- 相对时间（「下周一」）要锚定到材料自带的发生日期并保留原话；只有日历口径无歧义时才解析
     精确日期或区间，否则保留带锚点的表达而不编造端点。你的业务里还有什么时间口径要提醒编译注意？ -->

TODO：写下你的时间口径。

## 5. 隐私与不入册

<!-- 什么信息永远不进长期记忆？（密码、token、证件号是框架红线；
     你还想加什么——第三方的私事？健康与财务的推测？） -->

TODO：写下你的不入册清单。
