{"panel":{"identifier":"panel","description":"The bundled tray UI can receive state and open local console links. Launch at login is the app's own decision, settled in Rust, so the webview never reaches the autostart plugin directly.","local":true,"windows":["panel"],"permissions":["core:default","shell:allow-open"]}}